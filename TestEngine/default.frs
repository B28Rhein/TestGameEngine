#version 460 core
out vec4 FragColor;

uniform vec4 FragMod;
uniform int texAssigned;

in vec2 TexCoords;

uniform sampler2D tex;

void main()
{
	if(texAssigned == -1){
		FragColor = vec4(1, 1, 1, 1);
	}
	else {
	FragColor = texture(tex, TexCoords) * FragMod;
	}
};